torch
numpy
pyarrow
pyyaml
bitsandbytes
transformer-engine[pytorch]
tensorboard

# Tokenizer support (optional dependencies)
# Install based on your needs:
# - For byte-level tokenization (default): no additional dependencies needed
# - For TikToken (GPT-style): pip install tiktoken
# - For HuggingFace tokenizers: pip install transformers
# - For SentencePiece: pip install sentencepiece

# Uncomment the tokenizers you want to use:
tiktoken>=0.5.0  # GPT-style tokenization (gpt2, cl100k_base, etc.)
# transformers>=4.30.0  # HuggingFace tokenizers (BPE, WordPiece, etc.)
# sentencepiece>=0.1.99  # SentencePiece tokenization (multilingual)

